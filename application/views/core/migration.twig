{{ '<?php' }}
defined('BASEPATH') OR exit('No direct script access allowed');

class Migration_{{ name | capitalize }} extends CI_Migration {

        public function up()
        {
                $this->dbforge->add_field(array(
                        '{{ name }}_id' => array(
                                'type' => 'INT',
                                'constraint' => 5,
                                'unsigned' => TRUE,
                                'auto_increment' => TRUE
                        ),
                        '{{ name }}_title' => array(
                                'type' => 'VARCHAR',
                                'constraint' => '100',
                        ),
                        '{{ name }}_description' => array(
                                'type' => 'TEXT',
                                'null' => TRUE,
                        ),
                ));
                $this->dbforge->add_key('{{ name }}_id', TRUE);
                $this->dbforge->create_table('{{ name }}');
        }

        public function down()
        {
                $this->dbforge->drop_table('{{ name }}');
        }
}
